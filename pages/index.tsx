import type { NextPage } from "next";
import Head from "next/head";
import Image from "next/image";
import { Fragment } from "react";

import Button from "@mui/material/Button";
import { Box, fontSize, maxWidth } from "@mui/system";
import { Grid } from "@mui/material";

import { Swiper, SwiperSlide } from "swiper/react";
import SwiperCore, { Pagination, Navigation } from "swiper";
import "swiper/css";
import "swiper/css/pagination";
import "swiper/css/navigation";

import g_styles from "../styles/globals.module.css";
import Swiper_home from "../components/image/home-page/swiper";

SwiperCore.use([Pagination, Navigation]);

const Home: NextPage = () => {
  return (
    <main className={g_styles.main}>
      <Head>
        <title>Create Next App</title>
        <meta name="description" content="Generated by create next app" />
        <link rel="icon" href="/favicon.ico" />
      </Head>
      <div>
        <Swiper_home />
      </div>
      <div style={{ fontSize: "3vh", marginBottom: "2vh" }}>
        SPECIAL COLLABORATIONS
      </div>
      <div>
        <Grid
          container
          justifyContent="center"
          alignItems="center"
          rowSpacing={10}
        >
          <Grid item xs={12} md={6}>
            Women winter collections
            <Image
              src="/home/banner-1.jpg"
              alt="banner-1"
              width={770}
              height={900}
            />
          </Grid>
          <Grid item xs={12} md={6}>
            <div style={{ textAlign: "center" }}>
              {`"Being well-dressed hasn t much to do with having goodclothes. It's a question of good balance and good common sense."`}
              – Oscar de la Renta
            </div>
          </Grid>

          <Grid item xs={12} md={7}>
            <Image
              src="/home/banner-2.jpg"
              alt="banner-1"
              width={770}
              height={1000}
            />
          </Grid>
          <Grid item xs={12} md={6}>
            <div style={{ textAlign: "center" }}>
              &#8220Fashion is very important. It is life-enhancing and, like
              everything that gives pleasure, it is worth doing well.&#8221 –
              Vivienne Westwood
            </div>
          </Grid>
          <Grid item xs={12} md={6}>
            <Image
              src="/home/banner-3.jpg"
              alt="banner-1"
              width={1000}
              height={700}
            />
          </Grid>
        </Grid>
      </div>
    </main>
  );
};

export default Home;

export function getStaticProps() {
  // when one of the page is loaded, the props returned from getServerSide/StaticProps
  // of the current page will be automatically passed to the _app.js and merged to
  // the "pageProps". So we can tell the "navigation" inside the "Layout" that
  // which page we are currently in, then change the navigation bar accordingly

  return { props: { page: "home" } };
}
