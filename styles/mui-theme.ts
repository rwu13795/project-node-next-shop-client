import { createTheme } from "@mui/material";

export const theme = createTheme({
  palette: {
    // type: 'light',
    primary: {
      main: "#010106",
    },
    secondary: {
      main: "#bd0817",
    },
    error: {
      main: "#f44336",
    },
  },
});
